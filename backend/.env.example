# Optional: enables interactive, free-form Lunar AI Q&A via Claude.
# Without this set, Lunar AI still works fully using a transparent
# rule-based fallback grounded in the same computed data.
ANTHROPIC_API_KEY=
